<!DOCTYPE html>
<html>
  <head>
    <title>FadyProjectthree: <%= @page_title || "Admin area" %> </title>

  </head>

  <body>

    <% if !flash[:notice].blank? %>
      <div class="notice">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <!-- before yield -->
    <%= yield %>
    <!-- after yield -->
  </body>
</html>
